# Спектральна кластеризація довільних даних
  # Для ускладнення задачі кластеризації згенеруємо за допомогою функції make_circles() наступну задачу:
  # Найпростіший спосіб — побудувати k-граф найближчих сусідів (функція kneighbors_graph() з sklearn).